import {Loading} from "@nextui-org/react";

const LoadingComponent = ({imageURL, title, action, linkURL, cred}) => (
  <center>
    <Loading type="points" />
  </center>
);

export default LoadingComponent
